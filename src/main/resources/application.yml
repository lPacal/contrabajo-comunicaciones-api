spring:
  application:
    name: comunicaciones_api
  datasource:
    url: jdbc:sqlserver://localhost:58166;databaseName=MS_Comunicaciones;encrypt=true;trustServerCertificate=true;loginTimeout=30
    username: admincontrabajo
    password: Contrabajo123!
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jackson:
    serialization:
      fail-on-empty-beans: false
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
  config:
    import: "optional:configserver:http://localhost:8888"
  cloud:
    config:
      fail-fast: true
jwt:
  secret: EstaEsUnaClaveSecretaMuyLargaParaQueNadieLaAdivine123456789